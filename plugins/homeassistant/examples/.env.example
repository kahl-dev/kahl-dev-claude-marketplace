# Home Assistant Plugin - Environment Variables
# Copy to ~/.zshrc or ~/.bashrc and fill in your values

# ============================================
# REQUIRED - For all Home Assistant operations
# ============================================

# Your Home Assistant URL (usually http://homeassistant.local:8123)
export HOMEASSISTANT_URL="http://homeassistant.local:8123"

# Long-lived access token from HA
# Get it: Profile → Security → Long-Lived Access Tokens → Create Token
export HOMEASSISTANT_TOKEN="your-token-here"

# ============================================
# REQUIRED FOR CONFIG DEPLOYMENT
# ============================================

# SSH host alias or hostname for connecting to Home Assistant
# IMPORTANT: No default! Must be explicitly set.
# Example: "ha" (if configured in ~/.ssh/config)
# Example: "root@homeassistant.local" (direct)
export HA_SSH_HOST="your-ssh-alias"

# ============================================
# OPTIONAL - Override defaults if needed
# ============================================

# Local path for your HA config git repo
# Default: ~/ha-config
# export HA_LOCAL_CONFIG="~/ha-config"

# Path to HA config on the server
# Default: /homeassistant (HA OS)
# For Docker: might be /config or custom mount
# For Core: typically ~/.homeassistant
# export HA_CONFIG_PATH="/homeassistant"

# Staging directory for safe validation
# Default: /homeassistant/config_staging
# export HA_STAGING_PATH="/homeassistant/config_staging"
